name: the_crew_app
description: "The Crew - A vibrant community platform"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  go_router: ^15.1.2
  flutter_svg: ^2.0.17
  cached_network_image: ^3.4.1
  http: ^1.2.0
  socket_io_client: ^3.0.0
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  google_sign_in: ^6.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
